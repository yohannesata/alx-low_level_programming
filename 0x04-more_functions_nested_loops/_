#include <unistd.h>
#include <main.h>
/**
* _putchar - writes the characters c to stdout
* @c: the character is to print
*
* Return: on success 1.
* On error: 0 is returned
*/
int _putchar(char c)
{
	return (write(1, &c, 1));
}
