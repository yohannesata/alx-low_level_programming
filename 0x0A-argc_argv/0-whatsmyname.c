#include <stdio.h>
/**
 * main - writes its name
 * @argc: argument count
 * @argv: array of pointers to argument
 * return 0
 **/
int main(int argc, char *argv[])
{
	printf( "argv[0]: %s\n", argv[0]);
	return 0;
}
